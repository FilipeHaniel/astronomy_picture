import 'package:flutter/material.dart';

class SaveFullImage extends StatefulWidget {
  const SaveFullImage({super.key});

  @override
  State<SaveFullImage> createState() => _SaveFullImageState();
}

class _SaveFullImageState extends State<SaveFullImage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('')),
      body: Container(),
    );
  }
}
